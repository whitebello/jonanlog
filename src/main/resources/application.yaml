spring:
  application:
    name: jonanlog

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:jonanlog
    username: sa
    password:
    driver-class-name: org.h2.Driver
